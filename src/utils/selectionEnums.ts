export enum Gender {
    MALE = "MALE",
    FEMALE = "FEMALE",
    DIVERSE = "DIVERSE"
}

export enum Type {
    SEEKER = "SEEKER",
    OFFERER = "OFFERER"
}

export enum rentType {
    UNLIMITED = "unlimited",
    LIMITED = "limited"
}

export enum occupation {
    STUDENT = 'Student',
    WORKING = 'Working',
    ON_VACATION = 'On Vacation',
    OTHERS = 'Others'
}

export enum FlatshareExperience {
    NONE = 'None',
    LT_ONE = '≤ 1 year',
    GT_ONE = '> 1 year',
    GT_TWO = '> 2 year'
}

export enum flatshareType {
    STUDENTS_ONLY = 'students only',
    WORKERS_ONLY = 'workers only',
    STUDENT_ASSOCIATION = 'Student association',
    MIXED = 'mixed'
}

export enum genderRestrictions {
    WOMEN_ONLY = 'Women only',
    MEN_ONLY = 'Men only',
    None = 'None'
}
